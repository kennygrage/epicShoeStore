<html>
    <head>
        <title>Shoe Database</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css">
        <link rel="stylesheet" type="text/css" href="/../main.css">
    </head>
    <body>
        <div class="container">
            <h2>Stores</h2>
            {% if stores is not empty %}
                <p>Here are all your stores:</p>
                <ul class="list-group">
                    {% for store in stores %}
                        <li class="list-group-item"><a href="/stores/{{ store.getId }}">{{ store.getStoreName }}</a></li>
                    {% endfor %}
                </ul>
                {% endif %}

            <div class="submit-form">
                <form action='/stores' method='post'>
                    <label for='store_name'>Store Name</label>
                    <input id='store_name' name='store_name' type='text'>

                    <button class="btn btn-default" type='submit'>Add Store</button>
                </form>

                <form action='/delete_stores' method='post'>
                    <button class="btn btn-default" type='submit'>Clear</button>
                </form>
            </div>
            <p><a href='/'>Home</a></p>
        </div>
    </body>
</html>
